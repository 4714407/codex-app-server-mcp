# Changelog

本项目遵循语义化版本；重要变更记录于此。

## [1.0.0] - 2026-09-14

### Added

- 基于 stdio MCP 的 Codex app-server 桥接。
- 异步任务启动、状态增量查询、取消、模型列表、任务引导、原生审查和上下文压缩。
- 用户输入等待/回答、诊断工具、可选任务索引持久化。
- 本地与远程 app-server 连接，以及只读/工作区写入沙箱。

### Security

- 默认只读；可写模式要求 `CODEX_ALLOWED_ROOTS` 白名单。
- 拒绝命令、文件变更与权限审批请求；不自动重试中断任务。
