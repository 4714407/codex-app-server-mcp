# 安全策略

## 支持的版本

当前仅维护 `1.x` 主版本的最新发布版本。请先升级到最新版本后再报告问题。

## 报告漏洞

请不要通过公开 Issue、讨论区、提交记录或日志披露安全漏洞、Token、API Key、真实工作目录或未脱敏的提示词。

请将漏洞报告发送至 [4714407@qq.com](mailto:4714407@qq.com)，邮件主题建议以 `[codex-app-server-mcp security]` 开头。请尽量包含：

- 受影响的版本、操作系统、Node.js 与 Codex CLI 版本；
- 可复现步骤和预期/实际行为；
- 最小化的脱敏配置与日志；
- 潜在影响和已知缓解措施。

我们会在确认后回复，并在修复、发布和公开披露前协调处理时间。请不要在问题修复前公开披露细节。

## 安全边界

本项目默认只读。`workspace-write` 必须设置 `CODEX_ALLOWED_ROOTS`，且只允许任务 `cwd` 位于这些目录内。不要将远程 Token 写入 URL、提交到仓库或粘贴到 Issue。
